# Common

Shared app contracts that need to be understood by both the React client and Supabase functions live here once Project Management has persisted product features.

Keep this folder product-specific. General-purpose utilities that could reasonably become external packages belong in `lib`.
